# En desarrollo déjalo vacío: Vite hace proxy de /api hacia http://localhost:3000
# En producción (Netlify) apunta al backend de Render, incluyendo /api
VITE_API_URL=

# Client ID de Google (el mismo que en el backend). Vacío = sin botón de Google.
VITE_GOOGLE_CLIENT_ID=
